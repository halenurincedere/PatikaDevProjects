import React from "react";

const Logo = () => {
  const logoStyle = {
    fontSize: "1.8rem",
    fontWeight: "bold",
    color: "#fff",
    textDecoration: "none",
  };

  return (
    <a href="/" style={logoStyle}>
      POWERFULL
    </a>
  );
};

export default Logo;